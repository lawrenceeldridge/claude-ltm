# Generated files (plans and trackers)
docs/generated/
docs/whitepaper/

# Mined benchmark candidates — raw pre-review facts; must pass the human
# privacy gate before anything reaches dataset.json (see bench/mine_corpus.py)
plugins/engram/bench/candidates.json

# A/B run output — per-session usage + session ids (generated by bench/run_ab.py)
plugins/engram/bench/ab_results.json

# Claude per-user settings — settings.json is the team-shared file (versioned);
# settings.local.json is user-specific (gitignored by the monorepo root, listed
# here too for local clarity). Repo rooted at scaleapi/ips-applications:
# see the root .gitignore for `**/.claude/settings.local.json`.
.claude/settings.local.json
.cursor/settings.local.json

# Environment files — allow .env.example
.env
.env.*
!.env.example

__pycache__/
*.py[cod]
*.egg-info/
.DS_Store
*.db
*.sock
.venv/
venv/
